export class Personne {

  // tslint:disable-next-line: max-line-length
  constructor(public id: number, public nom: string, public prenom: string, public age: number, public profession: string, public avatar?: string){ }
}
